# TT-EG requirements (verified reproducible stack)
#
# Python 3.10, CUDA 12.1 GPU. These are the exact versions used to run the
# headline experiments (conda env `nlhf`). Mirrors EGPO/environment.yml.
#
# torch is the CUDA 12.1 build. Install torch FIRST from the cu121 index, then
# the rest, e.g.:
#
#   pip install torch==2.2.1 --index-url https://download.pytorch.org/whl/cu121
#   pip install -r requirements.txt
#
# (deepspeed builds against the installed torch/CUDA; keep CUDA_HOME set.)

# ── core training stack ──────────────────────────────────────────────
torch==2.2.1
transformers==4.48.0
trl==0.13.0
deepspeed==0.16.2
peft==0.14.0
accelerate==1.2.1
datasets==3.2.0

# ── HuggingFace runtime deps (pinned for reproducibility) ────────────
tokenizers==0.21.0
safetensors==0.5.2
huggingface-hub==0.27.1
sentencepiece==0.2.0

# ── numerics / logging ───────────────────────────────────────────────
numpy==1.26.4
scipy==1.12.0
tqdm==4.67.1
wandb==0.19.3

# ── plotting only (not needed for training; used by plot_*.py) ───────
matplotlib==3.10.8
